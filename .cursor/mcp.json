{
	"mcpServers": {
		"playwright": {
			"type": "stdio",
			"command": "bunx",
			"args": [
				"--bun",
				"@playwright/mcp@latest"
			],
			"description": "Local Playwright MCP. For SafeAppeals Electron: prefer attach to Run Dev (CDP) at http://127.0.0.1:9222 (--cdp-endpoint). Use launch.sh throwaway ports only when that desktop window is unavailable (see AGENTS.md and .claude/skills/launch)."
		}
	}
}
