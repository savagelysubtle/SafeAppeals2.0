# One Folder = One Case

Initializing a case folder creates:

- **`AGENTS.md`** — the case brief the agent reads at the start of every
  conversation
- **`.safeAppeals/case.json`** — the same facts in structured form
- **Standard case folders** (optional) for medical reports, correspondence,
  decisions, evidence, and unsorted intake
