# Install What’s Missing

If setup found missing search tools, install them when you are ready. The search
pack is about ~350 MB and stays on this computer. You will be asked to consent
before anything downloads.

You can skip this step and install later from Command Palette → **Private Search:
Install Missing Models**.

[Install Missing Models](command:safeappeals-rag.installMissingModels)
