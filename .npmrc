; Electron native module build settings
; Note: These configs are used by node-gyp/electron-rebuild for native modules
; npm 10+ shows warnings for these custom configs but they still work correctly
disturl="https://electronjs.org/headers"
target="34.3.2"
ms_build_id="11161073"
runtime="electron"
build_from_source="true"
legacy-peer-deps="true"
timeout=180000
